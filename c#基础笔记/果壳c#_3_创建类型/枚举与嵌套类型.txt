组合枚举
枚举值之间可以使用位运算符，进行组合
【flags】//标志枚举可以组合，如果不使用，仍然可以组合单输出的时候是一个数值而不是一组名称
enum xxs（如果可组合那么名字应该是复数的）{
a=1，b=2，c=4，d=8
}


a|b|c=7,根据数值按位或算出来的值

enum.isdefined检查枚举变量的值是否在枚举范围之内（检查合理性）


嵌套类型

嵌套里面的类型访问外层类型必须new一个外层类型才能访问