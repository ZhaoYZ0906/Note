定义：

装配件/程序集（dll/exe）:装配件是.Net应用程序执行的最小单位，编译出来的.dll、.exe都是装配件。
命名空间（namespace）:每一次创建一个新的项目（文件-新建），则产生一个新的命名空间
类（class）：定义一个class

三者之间的关系：一个装配件中可以存在多个命名空间，一个命名空间可以属于多个装配件，
一个类必须属于某一个命名空间，不同的命名空间下可以有同名类